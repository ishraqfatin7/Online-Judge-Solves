#include<bits/stdc++.h>
using namespace std;

int main() {

    int tc; 
    cin >> tc;
    while ( tc-- ) {
        int n; 
        cin >> n; 
        for (int i =1 ; i<n; i++) {
           float decimalNumber = i%10; 
           string  nString = to_string(i);
        }
    }

}