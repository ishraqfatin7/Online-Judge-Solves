#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define FAST                          \
    ios_base::sync_with_stdio(false); \
    cin.tie(0);

int main()
{
    int n, t;
    cin >> n >> t;
    if (n == 1 && t ==10)
    {
       cout<<-1<<endl;
       return 0;
    }
}