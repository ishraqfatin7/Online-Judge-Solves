#include<bits/stdc++.h>
using namespace std; 

int main(){

    int t;
    cin >>t; 
    while(t--){
        string s; 
        cin >> s; 
        sort(s.begin(),s.end());
        for (int i = 0; i < s.length(); i++)
        {
            if(s[i]==s[i+1]){
            
            }
        }
        
        cout << s << endl;
    }

}